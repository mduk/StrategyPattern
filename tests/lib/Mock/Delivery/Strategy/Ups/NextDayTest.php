<?php

namespace Mock\Delivery\Strategy\Ups;

require_once dirname(__FILE__) . '/../../../../../../lib/Mock/Delivery/Strategy/Ups/NextDay.php';

/**
 * Test class for NextDay.
 * Generated by PHPUnit on 2012-03-22 at 15:41:06.
 */
class NextDayTest extends \PHPUnit_Framework_TestCase {

	/**
	 * @var NextDay
	 */
	protected $object;

	/**
	 * Sets up the fixture, for example, opens a network connection.
	 * This method is called before a test is executed.
	 */
	protected function setUp() {
		$this->object = new NextDay;
	}

	/**
	 * Tears down the fixture, for example, closes a network connection.
	 * This method is called after a test is executed.
	 */
	protected function tearDown() {
		
	}

	public function testGetName()
	{
		$this->assertEquals( 'UPS Next Day (including Weekends)', $this->object->getName() );
	}

	public function testGetEstimate()
	{
		$estimate = $this->object->getEstimate(
			new \Mock\Delivery\Consignment,
			new \Mock\Delivery\Address
		);
		$this->assertInstanceOf( '\\Delivery\\Estimate', $estimate );
		$this->assertEquals( 28.75, $estimate->getCost() );
		$this->assertEquals( '1 Day', $estimate->getTime() );
	}

	public function testConfirm()
	{
		$estimate = $this->object->getEstimate(
			new \Mock\Delivery\Consignment,
			new \Mock\Delivery\Address
		);
		$result = $this->object->confirm( $estimate );
		$this->assertTrue( $result );
	}

}

?>
